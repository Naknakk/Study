/*
 
 Use the `displaySize` variable to choose between four display resolutions. Its value defaults to .fortyByForty. Be sure to set `displaySize` *first*, before any of your code.

 */

//displaySize = .twentyByTwenty
